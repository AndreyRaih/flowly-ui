export type RatingProps = {
    minRate: number
    maxRate: number
    minRateLabel: string
    maxRateLabel: string
} & Actionable

export function RatingComponent(props: RatingProps) {
    return <div></div>
}
